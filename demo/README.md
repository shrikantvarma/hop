# Demo workspace

This is a safe, fictional folder tree for recording `hop` demos.

From the repository root, run:

```zsh
HOPRC="$PWD/demo/hoprc" hop
```

The saved folders are `Code` and `Obsidian`. Nothing in this directory points
to your real files or uses your name.
